Requires a property in build.gradle: *ext.baseVersion* containing the version number without the build number

User must also specify:

- remote repo uer name
- remote repo name is taken to be project anme